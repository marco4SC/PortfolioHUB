"""Public data-source package.

The original implementation lives in ``data.souces`` (a legacy spelling).
These explicit modules provide the correctly named, stable import path used by
the API and agents without breaking existing references.
"""
